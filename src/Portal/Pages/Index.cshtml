@page
@model Portal.Pages.IndexModel

<partial name="_PageHead" model="@(new PageHeadModel(
    "Egress overview",
    "What the proxy is enforcing, what it is refusing, and whether it is healthy."))" />

<div class="grid g3" style="margin-bottom:16px">
    <partial name="_OverviewPolicy" model="Model" />
    <partial name="_OverviewTraffic" model="Model" />
    <partial name="_OverviewRuntime" model="Model" />
</div>

<partial name="_OverviewAttention" model="Model" />
