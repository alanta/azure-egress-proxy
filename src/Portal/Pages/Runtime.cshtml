@page
@model Portal.Pages.RuntimeModel

<partial name="_PageHead" model="@(new PageHeadModel(
    "Runtime",
    "One path, drawn once. Traffic arrives at the load balancer, crosses the proxy nodes and leaves "
    + "from the addresses in the egress prefix — the panels used to say this separately."))" />

<partial name="_Unavailable" model="Model.Error" />

@* The mockup puts a freshness stamp beside the title. Here it sits on the card and on each lane
   instead: the stages refresh independently on their own 60-second triggers and the page head does
   not, so a stamp up here would quietly go stale — which is the one thing a freshness stamp must
   never do. *@

<partial name="_RuntimePath" model="Model" />
